const { sagaFixture } = require('./add-saga.fixtures');

describe('App - Use Cases', () => {
  it('should add a saga with its movies', () => {
    conts
  });

  it('should throw error when required data is not sent');

  it('should throw database error when create saga fails');

  it('should throw database error when create movies fails');
});
